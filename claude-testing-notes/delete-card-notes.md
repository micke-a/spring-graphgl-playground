
> Add the ability to delete a card. I want to do this in two phases, where we first start implementing the service and persistence layer. Integration testing of this
without mocking and using the h2 database. Do not implement any controller or front-end changes.


> api-design-expert create a GraphQL muation to delete a card by its id.


> when viewing a customer, add a new Action column with a button to delete the relevant card